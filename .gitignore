# Build products (generated by `make` / mex)
/bin/
*.o
*.obj
*.d
O.*/

# EPICS site-local overrides (machine-specific; not for version control)
/RELEASE_SITE
/configure/*.local

# Pre-refactor backup of the old build files
/.build-backup-pre-epics-std/

